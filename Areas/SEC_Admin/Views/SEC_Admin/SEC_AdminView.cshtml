@{
    Layout = "~/Views/Shared/_AdminLayout.cshtml";
}
@using System.Data;
@using System.Data.SqlClient;
@using Event_Management.Areas.SEC_Admin;
@using Event_Management.BAL;
@model DataTable



    <div class="row justify-content-center">
        <div class="col-12">
            <div class="row">
                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountUsers"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Users</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-users text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountAdmins"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Admins</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-users text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountClients"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Clients</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-users text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountCustomer"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Customers</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-users text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>

            <div class="row">
                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountEvent"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Events</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-calendar text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountService"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Services</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-clipboard text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountVenues"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Venues</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-map-pin text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>

                <div class="col-md-6 col-xl-3 mb-4">
                    <div class="card shadow">
                        <div class="card-body">
                            <div class="row align-items-center">
                                <div class="col">
                                    @foreach (DataRow dr in Model.Rows)
                                    {
                                        <span class="h2 mb-0">@dr["CountComment"]</span>
                                    }
                                    <p class="small text-muted mb-0">Total Comments</p>
                                </div>
                                <div class="col-auto">
                                    <span class="fe fe-32 fe-align-left text-muted mb-0"></span>
                                </div>
                            </div>
                        </div>
                    </div>
                </div>
            </div>
        </div>
    </div>
    <h1>@CommonVariables.UserName()</h1>
